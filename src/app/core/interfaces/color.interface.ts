export class Color {
  name: string;
  code: string;
  icon: string;

  constructor(name: string, code: string, icon: string) {
    this.name = name;
    this.code = code;
    this.icon = icon;
  }
}
